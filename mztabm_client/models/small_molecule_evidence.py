# coding: utf-8

"""
    mzTab-M reference implementation and validation API.

    This is the mzTab-M reference implementation and validation API service.

    The version of the OpenAPI document: 2.0.0
    Contact: nils.hoffmann@cebitec.uni-bielefeld.de
    Generated by OpenAPI Generator (https://openapi-generator.tech)

    Do not edit the class manually.
"""  # noqa: E501


from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from pydantic import BaseModel, ConfigDict, Field, StrictFloat, StrictInt, StrictStr, field_validator
from typing import Any, ClassVar, Dict, List, Optional, Union
from typing_extensions import Annotated
from mztabm_client.models.comment import Comment
from mztabm_client.models.opt_column_mapping import OptColumnMapping
from mztabm_client.models.parameter import Parameter
from mztabm_client.models.spectra_ref import SpectraRef
from typing import Optional, Set
from typing_extensions import Self

class SmallMoleculeEvidence(BaseModel):
    """
    The small molecule evidence section is table-based, representing evidence for identifications of small molecules/features, from database search or any other process used to give putative identifications to molecules. In a typical case, each row represents one result from a single search or intepretation of a piece of evidence e.g. a database search with a fragmentation spectrum. Multiple results from a given input data item (e.g. one fragment spectrum) SHOULD share the same value under evidence_input_id.  The small molecule evidence section MUST always come after the Small Molecule Feature Table. All table columns MUST be Tab separated. There MUST NOT be any empty cells. Missing values MUST be reported using “null”.  The order of columns MUST follow the order specified below.  All columns are MANDATORY except for “opt_” columns. 
    """ # noqa: E501
    prefix: Optional[StrictStr] = Field(default='SME', description="The small molecule evidence table row prefix. SME MUST be used for rows of the small molecule evidence table.")
    header_prefix: Optional[StrictStr] = Field(default='SEH', description="The small molecule evidence table header prefix. SEH MUST be used for the small molecule evidence table header line (the column labels).")
    sme_id: StrictInt = Field(description="A within file unique identifier for the small molecule evidence result.")
    evidence_input_id: StrictStr = Field(description="A within file unique identifier for the input data used to support this identification e.g. fragment spectrum, RT and m/z pair, isotope profile that was used for the identification process, to serve as a grouping mechanism, whereby multiple rows of results from the same input data share the same ID. The identifiers may be human readable but should not be assumed to be interpretable. For example, if fragmentation spectra have been searched then the ID may be the spectrum reference, or for accurate mass search, the ms_run[2]:458.75.")
    database_identifier: StrictStr = Field(description="The putative identification for the small molecule sourced from an external database, using the same prefix specified in database[1-n]-prefix.  This could include additionally a chemical class or an identifier to a spectral library entity, even if its actual identity is unknown.  For the “no database” case, \"null\" must be used. The unprefixed use of \"null\" is prohibited for any other case. If no putative identification can be reported for a particular database, it MUST be reported as the database prefix followed by null. ")
    chemical_formula: Optional[StrictStr] = Field(default=None, description="The chemical formula of the identified compound e.g. in a database, assumed to match the theoretical mass to charge (in some cases this will be the derivatized form, including adducts and protons).  This should be specified in Hill notation (EA Hill 1900), i.e. elements in the order C, H and then alphabetically all other elements. Counts of one may be omitted. Elements should be capitalized properly to avoid confusion (e.g., “CO” vs. “Co”). The chemical formula reported should refer to the neutral form. Charge state is reported by the charge field.  Example N-acetylglucosamine would be encoded by the string “C8H15NO6” ")
    smiles: Optional[StrictStr] = Field(default=None, description="The potential molecule’s structure in the simplified molecular-input line-entry system (SMILES) for the small molecule.")
    inchi: Optional[StrictStr] = Field(default=None, description="A standard IUPAC International Chemical Identifier (InChI) for the given substance.")
    chemical_name: Optional[StrictStr] = Field(default=None, description="The small molecule’s chemical/common name, or general description if a chemical name is unavailable.")
    uri: Optional[StrictStr] = Field(default=None, description="A URI pointing to the small molecule’s entry in a database (e.g., the small molecule’s HMDB, Chebi or KEGG entry).")
    derivatized_form: Optional[Parameter] = None
    adduct_ion: Optional[Annotated[str, Field(strict=True)]] = Field(default=None, description="The assumed classification of this molecule’s adduct ion after detection, following the general style in the 2013 IUPAC recommendations on terms relating to MS e.g. [M+H]+, [M+Na]1+, [M+NH4]1+, [M-H]1-, [M+Cl]1-. If the adduct classification is ambiguous with regards to identification evidence it MAY be null.")
    exp_mass_to_charge: Union[StrictFloat, StrictInt] = Field(description="The experimental mass/charge value for the precursor ion. If multiple adduct forms have been combined into a single identification event/search, then a single value e.g. for the protonated form SHOULD be reported here.")
    charge: StrictInt = Field(description="The small molecule evidence’s charge value using positive integers both for positive and negative polarity modes.")
    theoretical_mass_to_charge: Union[StrictFloat, StrictInt] = Field(description="The theoretical mass/charge value for the small molecule or the database mass/charge value (for a spectral library match).")
    spectra_ref: List[SpectraRef] = Field(description="Reference to a spectrum in a spectrum file, for example a fragmentation spectrum has been used to support the identification. If a separate spectrum file has been used for fragmentation spectrum, this MUST be reported in the metadata section as additional ms_runs. The reference must be in the format ms_run[1-n]:{SPECTRA_REF} where SPECTRA_REF MUST follow the format defined in 5.2 (including references to chromatograms where these are used to inform identification). Multiple spectra MUST be referenced using a “|” delimited list for the (rare) cases in which search engines have combined or aggregated multiple spectra in advance of the search to make identifications.  If a fragmentation spectrum has not been used, the value should indicate the ms_run to which is identification is mapped e.g. “ms_run[1]”. ")
    identification_method: Parameter
    ms_level: Parameter
    id_confidence_measure: Optional[List[Union[StrictFloat, StrictInt]]] = Field(default=None, description="Any statistical value or score for the identification. The metadata section reports the type of score used, as id_confidence_measure[1-n] of type Param.")
    rank: Annotated[int, Field(strict=True, ge=1)] = Field(description="The rank of this identification from this approach as increasing integers from 1 (best ranked identification). Ties (equal score) are represented by using the same rank – defaults to 1 if there is no ranking system used.")
    opt: Optional[List[OptColumnMapping]] = Field(default=None, description="Additional columns can be added to the end of the small molecule evidence table. These column headers MUST start with the prefix “opt_” followed by the {identifier} of the object they reference: assay, study variable, MS run or “global” (if the value relates to all replicates). Column names MUST only contain the following characters: ‘A’-‘Z’, ‘a’-‘z’, ‘0’-‘9’, ‘’, ‘-’, ‘[’, ‘]’, and ‘:’. CV parameter accessions MAY be used for optional columns following the format: opt{identifier}_cv_{accession}_\\{parameter name}. Spaces within the parameter’s name MUST be replaced by ‘_’. ")
    comment: Optional[List[Comment]] = None
    __properties: ClassVar[List[str]] = ["prefix", "header_prefix", "sme_id", "evidence_input_id", "database_identifier", "chemical_formula", "smiles", "inchi", "chemical_name", "uri", "derivatized_form", "adduct_ion", "exp_mass_to_charge", "charge", "theoretical_mass_to_charge", "spectra_ref", "identification_method", "ms_level", "id_confidence_measure", "rank", "opt", "comment"]

    @field_validator('prefix')
    def prefix_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['SME']):
            raise ValueError("must be one of enum values ('SME')")
        return value

    @field_validator('header_prefix')
    def header_prefix_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in set(['SEH']):
            raise ValueError("must be one of enum values ('SEH')")
        return value

    @field_validator('adduct_ion')
    def adduct_ion_validate_regular_expression(cls, value):
        """Validates the regular expression"""
        if value is None:
            return value

        if not re.match(r"^\[\d*M([-][\w]*)\]\d*[+-]$", value):
            raise ValueError(r"must validate the regular expression /^\[\d*M([-][\w]*)\]\d*[+-]$/")
        return value

    model_config = ConfigDict(
        populate_by_name=True,
        validate_assignment=True,
        protected_namespaces=(),
    )


    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.model_dump(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        # TODO: pydantic v2: use .model_dump_json(by_alias=True, exclude_unset=True) instead
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> Optional[Self]:
        """Create an instance of SmallMoleculeEvidence from a JSON string"""
        return cls.from_dict(json.loads(json_str))

    def to_dict(self) -> Dict[str, Any]:
        """Return the dictionary representation of the model using alias.

        This has the following differences from calling pydantic's
        `self.model_dump(by_alias=True)`:

        * `None` is only added to the output dict for nullable fields that
          were set at model initialization. Other fields with value `None`
          are ignored.
        * OpenAPI `readOnly` fields are excluded.
        * OpenAPI `readOnly` fields are excluded.
        """
        excluded_fields: Set[str] = set([
            "prefix",
            "header_prefix",
        ])

        _dict = self.model_dump(
            by_alias=True,
            exclude=excluded_fields,
            exclude_none=True,
        )
        # override the default output from pydantic by calling `to_dict()` of derivatized_form
        if self.derivatized_form:
            _dict['derivatized_form'] = self.derivatized_form.to_dict()
        # override the default output from pydantic by calling `to_dict()` of each item in spectra_ref (list)
        _items = []
        if self.spectra_ref:
            for _item in self.spectra_ref:
                if _item:
                    _items.append(_item.to_dict())
            _dict['spectra_ref'] = _items
        # override the default output from pydantic by calling `to_dict()` of identification_method
        if self.identification_method:
            _dict['identification_method'] = self.identification_method.to_dict()
        # override the default output from pydantic by calling `to_dict()` of ms_level
        if self.ms_level:
            _dict['ms_level'] = self.ms_level.to_dict()
        # override the default output from pydantic by calling `to_dict()` of each item in opt (list)
        _items = []
        if self.opt:
            for _item in self.opt:
                if _item:
                    _items.append(_item.to_dict())
            _dict['opt'] = _items
        # override the default output from pydantic by calling `to_dict()` of each item in comment (list)
        _items = []
        if self.comment:
            for _item in self.comment:
                if _item:
                    _items.append(_item.to_dict())
            _dict['comment'] = _items
        return _dict

    @classmethod
    def from_dict(cls, obj: Optional[Dict[str, Any]]) -> Optional[Self]:
        """Create an instance of SmallMoleculeEvidence from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return cls.model_validate(obj)

        _obj = cls.model_validate({
            "prefix": obj.get("prefix") if obj.get("prefix") is not None else 'SME',
            "header_prefix": obj.get("header_prefix") if obj.get("header_prefix") is not None else 'SEH',
            "sme_id": obj.get("sme_id"),
            "evidence_input_id": obj.get("evidence_input_id"),
            "database_identifier": obj.get("database_identifier"),
            "chemical_formula": obj.get("chemical_formula"),
            "smiles": obj.get("smiles"),
            "inchi": obj.get("inchi"),
            "chemical_name": obj.get("chemical_name"),
            "uri": obj.get("uri"),
            "derivatized_form": Parameter.from_dict(obj["derivatized_form"]) if obj.get("derivatized_form") is not None else None,
            "adduct_ion": obj.get("adduct_ion"),
            "exp_mass_to_charge": obj.get("exp_mass_to_charge"),
            "charge": obj.get("charge"),
            "theoretical_mass_to_charge": obj.get("theoretical_mass_to_charge"),
            "spectra_ref": [SpectraRef.from_dict(_item) for _item in obj["spectra_ref"]] if obj.get("spectra_ref") is not None else None,
            "identification_method": Parameter.from_dict(obj["identification_method"]) if obj.get("identification_method") is not None else None,
            "ms_level": Parameter.from_dict(obj["ms_level"]) if obj.get("ms_level") is not None else None,
            "id_confidence_measure": obj.get("id_confidence_measure"),
            "rank": obj.get("rank") if obj.get("rank") is not None else 1,
            "opt": [OptColumnMapping.from_dict(_item) for _item in obj["opt"]] if obj.get("opt") is not None else None,
            "comment": [Comment.from_dict(_item) for _item in obj["comment"]] if obj.get("comment") is not None else None
        })
        return _obj


